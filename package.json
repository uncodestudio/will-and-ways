{
  "name": "will-and-ways",
  "version": "1.0.0",
  "type": "module",
  "description": "Will and Ways - Webflow JavaScript Modules",
  "scripts": {
    "dev": "vite",
    "build": "vite build && if not exist dist\\modules mkdir dist\\modules && copy modules\\*.js dist\\modules\\",
    "preview": "vite preview",
    "deploy": "npm run build && git add dist && git commit -m \"build: update dist\" && git push"
  },
  "keywords": ["webflow", "gsap", "vite"],
  "author": "Fabien Fournier - Uncode Studio",
  "license": "MIT",
  "devDependencies": {
    "vite": "^5.0.0"
  }
}